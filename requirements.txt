# CAFE — Python dependencies
#
# Pinned to the desktop profiling environment reported in the paper
# (Python 3.10.19, CUDA 12.8). The Jetson Orin Nano runs a different stack
# supplied by JetPack — see docs/ENVIRONMENT.md before installing there.

ultralytics==8.4.14
torch==2.10.0
torchinfo>=1.8.0

onnx>=1.16.0
onnxruntime-gpu==1.23.2

numpy>=1.24
pandas>=2.0
opencv-python>=4.8
PyYAML>=6.0

# TensorRT is installed from NVIDIA's distribution, not pip-pinned here.
# Desktop: TensorRT 10.15.1.29    Jetson: TensorRT 8.6.2 (JetPack)
